
class Build extends Project
{
    override function setup()
    {
        meta.target = SnowCLI;

        app.name      = 'Parcel';
        app.main      = 'Main';
        app.output    = 'bin';
        app.codepaths = [ '../../engine/src', 'src' ];

        build.dependencies = [
            'hxtelemetry'     => null,
            'tink_cli'        => null
        ];
    }
}
