package;

import buddy.Buddy;

class Tests implements Buddy<[
    tests.GdxParserTests,
    tests.RunCommandTests,
    tests.RestoreCommandTests,
    tests.CreateCommandTests,
    tests.BuildCommandTests,
    tests.PackerTests,
    tests.UtilsTests
]>
{
    //
}